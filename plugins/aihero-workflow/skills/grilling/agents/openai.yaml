interface:
  display_name: "Grilling"
  short_description: "Run a decision interview only when explicitly requested."
  default_prompt: "Use $grilling only when explicitly invoked for a bounded decision interview."
policy:
  allow_implicit_invocation: false
