interface:
  display_name: "Find Animation Opportunities"
  short_description: "Find animation opportunities only when explicitly requested."
  default_prompt: "Use $find-animation-opportunities only when explicitly invoked."
policy:
  allow_implicit_invocation: false
