interface:
  display_name: "Review Animations"
  short_description: "Review motion only when explicitly requested."
  default_prompt: "Use $review-animations only when explicitly invoked for an animation review."
policy:
  allow_implicit_invocation: false
