interface:
  display_name: "Emil Design Eng"
  short_description: "Use only for explicitly requested Emil-style design guidance."
  default_prompt: "Use $emil-design-eng only when explicitly invoked."
policy:
  allow_implicit_invocation: false
