interface:
  display_name: "Pick UI Library"
  short_description: "Choose a UI library only when explicitly requested."
  default_prompt: "Use $pick-ui-library only when explicitly invoked."
policy:
  allow_implicit_invocation: false
