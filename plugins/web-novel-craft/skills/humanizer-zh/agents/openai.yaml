interface:
  display_name: "Humanizer-zh"
  short_description: "识别并改写 AI 文本痕迹，同时保留原意、事实与语气"
  default_prompt: "Use $humanizer-zh to make this Chinese text sound natural while preserving its meaning, facts, and intended voice."
policy:
  allow_implicit_invocation: false
