interface:
  display_name: "Web Novel Characters"
  short_description: "Design durable characters, viewpoints, and relationships"
  default_prompt: "Use $web-novel-characters to strengthen the cast, viewpoints, and relationship arcs."
policy:
  allow_implicit_invocation: true
