# Define provider configuration under [providers.<name>]. Prefer env_key or
# command-backed authentication. Plaintext cross-device tokens are supported
# only as experimental_bearer_token and remain permanently visible in Git history.
